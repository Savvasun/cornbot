**Changes since last stable merge:**

* Split `$data` command into `$adddata`, `$changedata`, and `$removedata`, which separately let you change the contents of people.json

* More ratings and pickup lines